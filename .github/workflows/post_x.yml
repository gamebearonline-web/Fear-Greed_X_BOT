name: FearGreedBot - Post to X

on:
  workflow_dispatch:   # 手動実行
  # ※自動投稿は generate_image_save.yml の完了後に起動されるため
  # schedule は使わない（使うなら generate 側に移動）

permissions:
  contents: read

concurrency:
  group: post-feargreed
  cancel-in-progress: true

jobs:
  post-x:
    # 🔥 generate_image_save.yml の jobID に合わせる（これが一番重要）
    needs: generate-and-save

    runs-on: ubuntu-latest

    steps:
      # ======================
      # 1) リポジトリ取得
      # ======================
      - uses: actions/checkout@v4

      # ======================
      # 2) Python セットアップ（高速化）
      # ======================
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # ======================
      # 3) Python 依存パッケージ
      # ======================
      - name: Install deps
        run: pip install -r requirements.txt

      # ======================
      # 4) 投稿に必要な画像を取得
      #    generate_image_save.yml が push したものを使う
      # ======================
      - name: Load generated image from repo
        run: |
          ls -la output/
          test -f output/FearGreed_Output.png

      # ======================
      # 5) X に投稿
      # ======================
      - name: Post to X
        env:
          TWITTER_API_KEY:        ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET:     ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN:   ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET:  ${{ secrets.TWITTER_ACCESS_SECRET }}
          IMAGE_PATH: output/FearGreed_Output.png
        run: python post_x.py
