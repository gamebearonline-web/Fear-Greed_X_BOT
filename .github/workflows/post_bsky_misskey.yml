name: FearGreedBot - Post to Bluesky & Misskey

on:
  workflow_run:
    workflows: ["FearGreedBot - Post to X"]   # ← post_x.yml の name を指定
    types:
      - completed

permissions:
  contents: read

jobs:
  post-other-sns:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}   # X投稿が成功したときのみ実行
    runs-on: ubuntu-latest

    steps:
      # ======================
      # 1) リポジトリ取得
      # ======================
      - uses: actions/checkout@v4

      # ======================
      # 2) Python セットアップ
      # ======================
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install deps
        run: pip install -r requirements.txt atproto requests

      # ======================
      # 3) 画像チェック
      # ======================
      - name: Check generated image
        run: |
          echo "List output/"
          ls -la output/
          test -f output/FearGreed_Output.png

      # ======================
      # 4) Bluesky 投稿
      # ======================
      - name: Post to Bluesky
        env:
          BSKY_HANDLE:        ${{ secrets.BSKY_HANDLE }}
          BSKY_APP_PASSWORD:  ${{ secrets.BSKY_APP_PASSWORD }}
          IMAGE_PATH: output/FearGreed_Output.png
        run: python post_bluesky.py

      # ======================
      # 5) Misskey 投稿
      # ======================
      - name: Post to Misskey
        env:
          MISSKEY_HOST:  ${{ secrets.MISSKEY_HOST }}
          MISSKEY_TOKEN: ${{ secrets.MISSKEY_TOKEN }}
          IMAGE_PATH: output/FearGreed_Output.png
        run: python post_misskey.py
